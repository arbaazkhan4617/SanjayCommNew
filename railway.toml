[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && java -jar target/integrators-backend-1.0.0.jar"
healthcheckPath = "/api/products/services"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
