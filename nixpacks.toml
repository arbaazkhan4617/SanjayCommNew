[phases.setup]
nixPkgs = ["maven", "jdk17"]

[phases.install]
cmds = ["cd backend && mvn clean install -DskipTests"]

[phases.build]
cmds = ["cd backend && mvn package -DskipTests"]

[start]
cmd = "cd backend && java -jar target/integrators-backend-1.0.0.jar"
